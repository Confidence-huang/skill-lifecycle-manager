interface:
  display_name: "Skill Lifecycle Manager"
  short_description: "Inventory, stabilize, check, and govern Skill assets"
  default_prompt: "Use $skill-lifecycle-manager to inspect Skill assets, maintain evidence-backed governance, freeze or health-check a stable baseline, and perform only the explicitly requested lifecycle operation."
